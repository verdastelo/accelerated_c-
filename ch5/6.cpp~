#include <iostream> 

void display (int num[10])
{
    for (int i = 0; i < 10; i++) {
        std::cout << num[i] << std::endl; 
    }
 }
 
int main() 
{
    int t[10]; 
    
    for (int i = 0; i < 10; ++i) {
        t[i] = i; 
        }
    
    display(t); 
    
    return 0; 
}
