/*
    Show the general form of a function.
*/

type function_name (parameter_1, paramter_2... parameter_n) 
{
    function_body;
    return type;
}

type: int, float, double, long, char
function_name: anything other than one of the reserved keywords
paramter_x: values the function operates on
function_body: a bunch of statements
return type: the same as the first type

